import AboutContent from "../../components/AboutContent";

export const metadata = {
  title: "About Us — Acentics",
  description:
    "Acentics is a senior, full-service marketing team — strategists, creatives, and analysts embedded directly with your business.",
};

export default function AboutPage() {
  return <AboutContent />;
}
