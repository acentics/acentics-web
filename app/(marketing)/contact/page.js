import ContactContent from "../../components/ContactContent";

export const metadata = {
  title: "Contact Us — Acentics",
  description: "Get in touch with Acentics — send us a message, find direct contact info, or find the right team for your question.",
};

export default function ContactPage() {
  return <ContactContent />;
}
