import BlogContent from "../../components/BlogContent";

export const metadata = {
  title: "Blog — Acentics",
  description: "Notes from the Acentics team on strategy, growth, and what's actually working across our client accounts.",
};

export default function BlogPage() {
  return <BlogContent />;
}
